function firstName(name){
    let first;
    for(i = 0 ; i < name.length ; i ++){
        if(name[i] != ' '){
            first[j] = name[i];
        }

        else{
            console.log(first);
            return first;
        }
    }
    console.log(first);
    return first;
}

a = firstName('Manish');
print(a)